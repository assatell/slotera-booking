import fs from 'node:fs';import assert from 'node:assert/strict';const q=r=>fs.readFileSync(new URL('../../'+r,import.meta.url),'utf8');const a=q('includes/Frontend/Shortcodes/BookingShortcode.php'),j=q('assets/js/frontend-booking-form.js'),c=q('assets/css/frontend.css'),h=q('includes/Core/HtmlSanitizer.php');assert.doesNotMatch(a,/data-sltr-video-unmute|Turn on sound/);assert.doesNotMatch(j,/data-sltr-video-unmute|video\.muted = false|video\.volume = 1/);assert.doesNotMatch(c,/\.sltr-package-video-unmute/);assert.doesNotMatch(h,/'data-sltr-video-unmute' => true/);console.log('autoplay video uses native player controls only: ok');
